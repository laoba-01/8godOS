# 2026-09-10 — M3 VGA 打印

> 会话 ID `90ab2f87`｜队员消息 10 条 / AI 回复 19 条
> **AI 回复已截断至 600 字符**，完整原文见 `原始记录/90ab2f87*.jsonl`。
> 时间戳为 UTC（本地为 +8 小时）。

---

**[2026-09-10 10:36] 队员：**

帮我看看下一步该怎么做?

**[2026-09-10 10:37] AI：**

我先看一下当前项目结构和代码状态,再给你下一步的建议。

**[2026-09-10 10:38] AI：**

看完了。你的现状很清楚:**M2(长模式)的代码已经写进 `boot.asm` 了,而且我审阅下来基本正确**,下一步就是 **M3:写 `kernel.c`,在 VGA 屏幕打印 `Hello, kernel!`**。 不过有个**先决卡点**要先提醒你: ## ⚠️ 当前 `make` 会直接失败 `src/` 目录下现在只有 `boot.asm` 和 `linker.ld`,**没有 `kernel.c`**。而这两处都已经引用了它: - `boot.asm:91` 的 `extern kmain` + `call kmain` - `Makefile:28` 的 `$(BUILD_DIR)/kernel.o: src/kernel.c` 所以下一步的实质工作就是把 `kernel.c` 补出来,让链接通过、屏幕出字。 ## 对 boot.asm 的审阅(先给结论:能跑) 长模式切换顺序、页表(前 2MB 恒等映射、2MB 大页)、64 位 GDT、段寄存器设置都对。只有两个小点,现在不影响 M3,记一下即可: 1. **没保存 EBX**(multiboot2 信息指针)。`计划书` 第 4 节写了"保存 EBX",但代码里 `_start` 校验完 EAX 后直接往下走,没存。M3 用不到,但 M4 以后做键盘/内存映射时会需要——到时候再补。 2. 缩进有几处乱(boo …（完整内容见原始记录）

**[2026-09-10 11:13] 队员：**

那我的kernel.c怎么写?

**[2026-09-10 11:14] AI：**

按你的约定(亲手写、我只指导),我不给你成品代码,给你一份**能照着写出来的要点清单 + 伪代码**。写完发我审。 ## kernel.c 就两样东西:一个入口 + 一个打印函数 ### 1. 文件骨架 ```c #include <stdint.h> /* 可选。freestanding 下 gcc 自带,给你 uint8_t/uint16_t */ /* 你的打印函数(先声明或直接定义在 kmain 前面) */ static void vga_puts(const char *s) { ... } void kmain(void) { vga_puts("Hello, kernel!"); for (;;) { __asm__ volatile("hlt"); } } ``` 要点: - `kmain` 必须是 `void kmain(void)`——boot.asm 里 `call kmain` 不传参,调用完它自己 hlt,所以 kmain **不能 return、也别依赖 argc/argv**。 - 结尾用 `for(;;)` + `hlt`,别用空 `for(;;);`(那是忙等,CPU 满载空转)。 ### 2. 核心事实(VGA 文本模式) | 项 | 值 | |---|---| | 缓冲区基地址 | `0xB8000` | | 分辨率 | 80 列 × 25  …（完整内容见原始记录）

**[2026-09-10 11:34] 队员：**

帮我看看是这样吗

**[2026-09-10 11:34] AI：**

方向对了,但有个关键点:**`vga_puts` 的 `{ ... }` 里的 `...` 是我给的占位符,不是合法 C 代码**——现在这样 `make` 会在编译期直接报语法错误。你的骨架抄对了,但函数体还没写。 逐行看: | 行 | 内容 | 状态 | |---|---|---| | 1 | `#include <stdint.h>` | ✅ 没问题 | | 3 | 注释 | ✅ 只是注释,不影响 | | 4 | `static void vga_puts(...) { ... }` | ❌ **`...` 不是 C 语法,要换成真正的函数体** | | 6-9 | `kmain` | ✅ 签名、`vga_puts("Hello, kernel!")`、`hlt` 死循环,全对 | 所以现在只差一件事:**把 `{ ... }` 换成真正的打印逻辑**。 对照我上一轮给你的伪代码,函数体里你需要亲手写这 4 块: 1. **声明指针**:一个 `volatile` 的 VGA 缓冲区指针(指到 `0xB8000`)。 2. **一个位置变量**:记录当前写到第几个字符,比如 `int pos = 0`(局部变量即可,因为你的函数只被调一次)。 3. **一个循环**:`while (*s != '\0')`,里面: - 取当前字符 `char ch = *s;` - 把 `c …（完整内容见原始记录）

**[2026-09-10 11:40] 队员：**

帮我看看现在

**[2026-09-10 11:40] AI：**

审完了。**循环骨架和 `\n` 处理都写对了**,但有两个问题——一个是编译错误,一个是**最核心的"往屏幕写"那步整个漏了**。 ## 问题 1:编译错误——`ch` 没声明 `ch=*s;` 里用了 `ch`,但你从没声明它。要在函数体开头(和 `int pos=0;` 并列)加一句: ```c char ch; ``` 不然 `make` 直接报 `'ch' undeclared`。 ## 问题 2:核心缺失——没往 VGA 写任何东西(最重要) 整个函数里现在**没有 VGA 指针、没有任何写内存的动作**。你的 `else` 分支是: ```c else{ pos=pos+1; // 只把位置 +1,啥也没写 } ``` 这就像"说好要发消息,结果只把光标往前挪了一格,一个字都没打"。就算 `ch` 声明了、编译过了,屏幕上**什么都不会出现**。 你漏了两个东西: **① 函数开头要声明 VGA 指针**(指向 `0xB8000`): ```c volatile unsigned char *vga = (volatile unsigned char *)0xB8000; ``` **② `else` 分支里要真正把字符写进去**,而且顺序是"先在当前位置写,再挪位置": ```c else{ vga[pos*2] = ch; // 低字节 = ASCII vga[p …（完整内容见原始记录）

**[2026-09-10 11:45] 队员：**

现在呢

**[2026-09-10 11:45] AI：**

就差最后一步了。**`ch` 和写操作都对了,但 `vga` 这个指针你还没声明**——现在 `make` 会报 `'vga' undeclared`,因为第 15-16 行用了 `vga`,但全函数里没有它的定义。 这就是我上一条说的"问题 2 的 ①",你漏掉了。在 `vga_puts` 函数开头、`int pos=0;` 旁边加这一行: ```c volatile unsigned char *vga = (volatile unsigned char *)0xB8000; ``` 加完应该长这样(开头部分): ```c static void vga_puts(const char *s) { volatile unsigned char *vga = (volatile unsigned char *)0xB8000; int pos = 0; while (*s != 0) { char ch = *s; ... ``` 其余全都不用动——`ch` 声明 ✅、写 ASCII + 颜色 ✅、"先写后挪位置"的顺序 ✅、`\n` 换行 ✅ 都已经对了。 补上这行就完整了,`make` + `make run` 应该能出字。出了再告诉我。

**[2026-09-10 11:48] 队员：**

OK了吧,跑一下测试

**[2026-09-10 11:48] AI：**

代码完整了,`vga` 声明也补上了。现在试着跑构建。先确认一下当前环境里有哪些工具能用(你的工具链在 WSL 里,我在 Windows 侧,得先探一下)。

**[2026-09-10 11:48] AI：**

工具链确实在 WSL 里(Windows 侧只有 MinGW 的 gcc/ld,就是计划书里明确说"别用"的那个 PE 版),`wsl.exe` 可用。我用 WSL 来构建。先确认 WSL 发行版和工具是否就位:

**[2026-09-10 11:48] AI：**

工具齐了。做一次干净构建(`make clean` + `make`):

**[2026-09-10 11:49] AI：**

构建成功——`kernel.c` 编译零警告,链接、ISO 生成全通过(那条 `missing .note.GNU-stack` 是 ld 对 nasm 产物的无害提示,OS 内核里正常)。 现在真正启动 QEMU 验证屏幕出字。先看一下 GRUB 配置的启动超时,好确定等多久:

**[2026-09-10 11:49] AI：**

`timeout=0` 即秒进。现在启动 QEMU 无头跑 5 秒,用 monitor 把 VGA 显存(0xB8000)导出来看:

**[2026-09-10 11:51] AI：**

QEMU 起来了,但 `-monitor stdio` 走管道时 readline 把命令搞乱了(报 `invalid char 't'`,文件没生成)。换成 unix socket monitor + Python 客户端,干净地发命令:

**[2026-09-10 11:52] AI：**

同样的错——所以不是管道的问题,是 `pmemsave` 命令本身对参数解析出错(它把 `/tmp/...` 里的 `t` 当成了表达式的一部分,这是 QEMU 8.2 的一个已知怪癖)。换 `xp` 直接读物理内存,把显存 hex 打到 monitor 输出里:

**[2026-09-10 11:53] AI：**

🎉 **成功了!屏幕出字了,而且字节级完全正确。** QEMU 无头跑 5 秒后,把 VGA 显存 `0xB8000` 导出来,逐字节解码: | 偏移 | 十六进制 | 解码 | |---|---|---| | 0xB8000 | `48 07` | `H`(灰字黑底 `0x07`) | | 0xB8002 | `65 07` | `e` | | 0xB8004 | `6c 07` | `l` | | 0xB8006 | `6c 07` | `l` | | 0xB8008 | `6f 07` | `o` | | 0xB800A | `2c 07` | `,` | | 0xB800C | `20 07` | ` ` | | 0xB800E | `6b 07` | `k` | | 0xB8010 | `65 07` | `e` | | 0xB8012 | `72 07` | `r` | | 0xB8014 | `6e 07` | `n` | | 0xB8016 | `65 07` | `e` | | 0xB8018 | `6c 07` | `l` | | 0xB801A | `21 07` | `!` | → **`Hello, kernel!`** 一个字符不差,颜色字节全是 `0x07`(灰字黑底),也正确。qemu 日志尾部为空 = 没有 triple fault,**干净启动* …（完整内容见原始记录）

**[2026-09-10 11:54] 队员：**

那为什么vga是0xB8000

**[2026-09-10 11:54] AI：**

好问题。一句话:**`0xB8000` 不是我们选的,是 IBM PC 兼容机的硬件标准——显卡把这块物理内存硬编码成了"文本模式的显存"。** ## 1. 历史来源:1981 年的 IBM PC 最早的 IBM PC(8088,20 位地址线,只能寻址 1MB)内存布局是固定的,顶部 384KB 不装 RAM,而是留给显卡和 BIOS: ``` 0x00000 ─────────────────────── 常规内存 640KB(RAM) 0x9FFFF 0xA0000 ─────────────────────── 显卡内存区(128KB) 0xA0000 ~ 0xAFFFF 图形模式帧缓冲(EGA/VGA) 0xB0000 ~ 0xB7FFF 单色文本模式(MDA,绿/琥珀字) 0xB8000 ~ 0xBFFFF 彩色文本模式(CGA/EGA/VGA) ← 就是这里 0xC0000 ─────────────────────── 显卡 BIOS ROM + 其它适配卡 ROM 0xFFFFF 系统 BIOS ROM ``` `0xB8000` 是当年 **CGA(彩色图形适配器)**的文本模式显存地址。后来的 EGA、VGA 为了向后兼容,一直保留这个地址——你现在的 QEMU 虚拟显卡,开机时也是以兼容 VGA 文本模式(80×25,模式 3)存在的,所以 `0xB8000 …（完整内容见原始记录）

**[2026-09-10 11:57] 队员：**

讲一讲现在这个项目涉及到哪些知识点?

**[2026-09-10 11:57] AI：**

这个项目麻雀虽小,但"从通电到屏幕出字"这一段,几乎横跨了 OS 开发最底层的一整套知识。我按领域拆给你看,每条都对应你项目里的具体代码。 ## 一、引导与启动:内核怎么"活"过来 | 知识点 | 在你项目里的体现 | |---|---| | **计算机开机流程** | BIOS → GRUB → 内核(README 第 3 行那条目标链) | | **Multiboot2 规范** | `boot.asm:1-12`:header 魔数 `0xE85250D6`、checksum 计算、终止 tag;`boot.asm:20` 校验引导魔数 `0x36D76289` | | **ELF 可执行文件格式** | `linker.ld` 指定 `OUTPUT_FORMAT(elf64-x86-64)`,GRUB 就是靠解析 ELF 段来加载内核 | | **链接脚本(linker script)** | `linker.ld` 决定内核被加载到 `0x100000`(1MB),并保证 `.multiboot2` 段在最前(规范要求前 32768 字节内) | ## 二、x86_64 CPU 架构(最硬核的一块) | 知识点 | 体现在 | |---|---| | **三种运行模式** | 实模式(16)→ 保护模式(32,GRUB 进入)→ 长模式(64)。`boot.asm` 用  …（完整内容见原始记录）

**[2026-09-10 12:26] 队员：**

看一下能不能commit并且push

**[2026-09-10 12:27] AI：**

当前状态清楚了: - `src/kernel.c` 是新文件(未跟踪) - `README.md` 是你自己改的(只动了 boot.asm 那行描述) - 有远程 `origin` → `github.com/laoba-01/8godOS`,分支与 origin/master 同步,没有分叉,**可以干净地 commit + push** `build/` 已被 `.gitignore` 忽略,不会混进去。我按你之前的提交风格来:

**[2026-09-10 12:30] AI：**

✅ **commit + push 都完成了。** ``` 1f3569d M3: VGA 打印 Hello, kernel! (kernel.c: kmain + vga_puts 写 0xB8000 显存) b0186ac..1f3569d master -> master (已推到 origin) ``` - `src/kernel.c` 新建提交 - `README.md` 你那行描述改动一起带上 - `build/` 被 `.gitignore` 挡住,没混进去 ## 一个小尾巴:README 里程碑表还是旧的 你这次只改了目录树里 boot.asm 那行注释,但**顶部的里程碑表没动**,现在和实际不符: | 里程碑 | 表里写的 | 实际 | |---|---|---| | M2 长模式 | 🚧 进行中 | 已完成并已提交 | | M3 VGA 打印 | ⏳ | ✅ 已验证出字 | 这是文档,不算内核代码。要不要我帮你把这两格改成 ✅、并加个"完成日期",再补一个 `docs:` 的小 commit?你点头我就动。

**[2026-09-10 12:38] 队员：**

那readme能不能更新一下

**[2026-09-10 12:39] AI：**

✅ **README 更新并推上去了。** ``` a4fd49a docs: 更新里程碑状态 (M2/M3 已完成) → 已推到 origin/master ``` 里程碑表现在是: | 里程碑 | 状态 | |---|---| | M0 环境就绪 | ✅ 完成 | | M1 能引导 | ✅ 完成 | | M2 进入 64 位长模式 | ✅ 完成 | | M3 VGA 打印 `Hello, kernel!` | ✅ 完成 | | M4 一键构建脚本 | ⏳ | **项目总目标(引导 → 长模式 → 屏幕出字)已全部达成。** 当前远程 master 上的提交历史: ``` a4fd49a docs: 更新里程碑状态 1f3569d M3: VGA 打印 Hello, kernel! b0186ac M2: 进入 64 位长模式 f22347e docs: 添加 README 5724b8a M1: 最小内核可引导 ``` --- 顺带提一句:`计划书.md` 第 155 行还写着"当前进度:M0 已完成 ✅ → 下一步 M1",也是旧信息。要不要一起更新成"当前进度:M3 已完成 ✅ → 下一步 M4"?还有它的里程碑章节(M2/M3 还没写状态)。你说了我再动。
