# 2026-09-12 — 引导代码答疑

> 会话 ID `6008442d`｜队员消息 5 条 / AI 回复 5 条
> **AI 回复已截断至 600 字符**，完整原文见 `原始记录/6008442d*.jsonl`。
> 时间戳为 UTC（本地为 +8 小时）。

---

**[2026-09-12 05:36] 队员：**

BIOS写完了吗

**[2026-09-12 05:37] AI：**

不是的——BIOS 不在这个项目要写的范围内。 看 `计划书.md` 的启动流程(第 4 节): ``` BIOS └─> GRUB(读 multiboot2 头 → 加载内核 ELF) └─> _start(汇编) └─> kmain(C) ``` **BIOS 是硬件固件**，不是我们写的代码。在 QEMU 里跑的时候，BIOS 由 QEMU 自带的 SeaBIOS 提供，开机后它只负责去加载 GRUB（引导程序）。这个项目里我们自己写的、能称为"引导相关"的只有： | 文件 | 作用 | |------|------| | `src/boot.asm` | multiboot2 头 + 长模式切换（32→64）+ 64 位入口 | | `grub/grub.cfg` | GRUB 菜单配置 | | `src/linker.ld` | 链接脚本 | 所以整个链是：**BIOS（固件，我们不碰）→ GRUB（现成的引导器）→ boot.asm（我们的代码）→ kmain**。 当前里程碑 M0~M4 已全部完成，`boot.asm` 这部分（长模式切换、进 64 位）已经写完了。 你是把 BIOS 和 boot.asm 搞混了，还是想自己动手写一个 Bootloader（用汇编直接接管 BIOS 引导，绕开 GRUB）？如果是后者，那是一个新的方向，可以聊聊。

**[2026-09-12 05:43] 队员：**

运行成功的界面怎么看?

**[2026-09-12 05:44] AI：**

在 **WSL2 Ubuntu** 终端里跑，就能弹出一个 QEMU 窗口看到结果。步骤如下： ## 步骤 ```bash # 1. 进 WSL（在 Windows 终端里输入 wsl 回车） wsl # 2. 进项目目录 cd /mnt/d/8GodOS # 3. 编译 + 启动 QEMU make run ``` `make run` 会先自动编译、生成 ISO，然后执行 `qemu-system-x86_64 -cdrom build/os.iso`，弹出一个 **QEMU 窗口**。 ## 成功界面长这样 QEMU 窗口里是一块**黑屏**，左上角显示一行灰白色文字： ``` Hello, kernel! ``` 看到这行字就是成功了（对应 M3 里程碑的验收标准）。 ## 如果窗口没弹出来（无 GUI 显示） Windows 11 的 WSL2 默认带 WSLg，QEMU 窗口一般能直接弹。如果弹不出来，改用纯文本模式跑，输出会直接打到终端里： ```bash qemu-system-x86_64 -cdrom build/os.iso -nographic ``` ⚠️ 注意：`-nographic` 会把 GRUB 菜单和内核输出都重定向到终端，可能看到一串终端控制码。更干净的方式是走免 ISO 的直载捷径（跳过 GRUB 菜单，直接进内核）： ```bash mak …（完整内容见原始记录）

**[2026-09-12 05:45] 队员：**

为什么不在ubuntu里?

**[2026-09-12 05:46] AI：**

好问题——关键在于：**当前这个 Claude Code 会话是跑在 Windows 上的，不是 Ubuntu**。 看环境信息：工作目录是 `D:\8GodOS`（Windows 盘符），Shell 是 Git Bash（win32）。而编译内核需要的工具链全装在 **WSL2 Ubuntu** 里。所以「构建 + 运行」这一步必须在 Ubuntu 里做，原因有三个： ## 1. 工具链装在 WSL Ubuntu 里 `计划书.md` 第 3 节核实过：`nasm`、`gcc`、`ld`、`grub-mkrescue`、`qemu-system-x86_64` 都是 `sudo apt install` 装到 WSL Ubuntu 的。Windows 原生没有这些。 ## 2. 内核必须是 ELF64，Windows 编不出来 Windows 上的 gcc 是 MinGW，输出的是 **PE（.exe）格式**，链接都会报错。而 GRUB 只认 **ELF64** 格式的内核——这是 Linux 的 `gcc`/`ld` 才能正确产出的。`计划书.md` 明确写了： > Windows 原生 MinGW gcc 8.1（PE 目标）⚠️ 不适合编内核，不用 ## 3. 文件是两边共享的，所以能「Windows 编辑、Ubuntu 编译」 项目放在 `D:\8GodOS`，WSL  …（完整内容见原始记录）

**[2026-09-12 05:47] 队员：**

wslg [ ~ ]$ ls
wslg [ ~ ]$ ls
wslg [ ~ ]$ ls
wslg [ ~ ]$ cd ..
wslg [ /home ]$ ls
nonroot  wslg
wslg [ /home ]$ ls
nonroot  wslg
wslg [ /home ]$ wsl -e bash -c "cd /mnt/d/8GodOS && make"
-bash: wsl: command not found
wslg [ /home ]$

**[2026-09-12 05:48] AI：**

你卡在两个小误区上了，我帮你理清： ## 1. `wsl` 只能在 Windows 里敲，你现在已经在 Linux 里了 看提示符 `wslg [ ~ ]$`——这是 Linux 的 bash 提示符。`wsl` 是**Windows 的启动命令**，用来从 Windows 进入 Linux；进了 Linux 之后自然就没有 `wsl` 这个命令了，所以报 `command not found`。 ## 2. 你进错了发行版——这是 WSLg 系统盘，不是 Ubuntu 关键证据在 `/home` 目录： ``` nonroot wslg ``` 这个 `wslg` 用户 + `/home/wslg`，是 **WSLg 的系统发行版**（WSL 用来支持图形界面的隐藏系统盘），**不是**你装工具链的那个 Ubuntu 24.04。所以这里面既没有 `/mnt/d/8GodOS` 的完整环境，也没有 nasm/qemu 等工具。 ## 正确做法 **第一步：退出当前这个 wslg shell** ```bash exit ``` 一直退到回到 **Windows 提示符**（看到 `PS C:\...>` 或 `C:\...>` 为止）。 **第二步：在 Windows 里查你的发行版** ```powershell wsl --list --verbose ``` 会列出类似：  …（完整内容见原始记录）

**[2026-09-12 05:50] 队员：**

可以用wsl UI进Ubuntu吗

**[2026-09-12 05:50] AI：**

可以，而且这反而是**最不容易进错盘**的方式。 ## 方法 1：开始菜单直接点 Ubuntu 图标（最简单） 1. 按 `Win` 键（或点左下角开始菜单） 2. 直接输入 `Ubuntu` 3. 会看到一个 **Ubuntu**（或 `Ubuntu 24.04 LTS`）的图标 4. 点它 → 直接弹出一个终端窗口，**已经是正确的 Ubuntu**，不会再掉进 wslg 那个系统盘 进去后直接： ```bash cd /mnt/d/8GodOS make run ``` ## 方法 2：Windows Terminal 下拉菜单 1. 打开 **Windows Terminal**（Win 键搜 `Terminal`） 2. 点标签栏右边的下拉箭头 `∨` 3. 选 **Ubuntu** → 新开一个 Ubuntu 标签页 --- ⚠️ 一个小提醒：刚才你 `wsl` 会掉进 wslg，通常是因为**默认发行版被设错了**。用开始菜单点 Ubuntu 图标能绕开这个问题（它是指定发行版启动的）。 如果你希望以后敲 `wsl` 就直接进 Ubuntu（而不是 wslg），可以在 Windows PowerShell 里设一下默认： ```powershell wsl --set-default Ubuntu-24.04 ``` （名字以 `wsl --list --verbos …（完整内容见原始记录）
