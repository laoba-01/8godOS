# 2026-09-15 — M5 收尾

> 会话 ID `5e1b06ba`｜队员消息 1 条 / AI 回复 1 条
> **AI 回复已截断至 600 字符**，完整原文见 `原始记录/5e1b06ba*.jsonl`。
> 时间戳为 UTC（本地为 +8 小时）。

---

**[2026-09-15 08:25] 队员：**

看看M5还差什么

**[2026-09-15 08:27] AI：**

看完了。M5 的清单里，**功能上只剩硬件光标这一项**，其余是收尾验证。 ## 已完成 ✅ | 验证标准 | 状态 | |---|---| | 1. `make` 零警告（`-Wall -Wextra`） | ✅ 刚在 WSL 里重编 `vga.o`/`kernel.o`，无任何警告 | | 2. 整屏上滚 | ✅ `vga_scroll()` 已实现，`vga_newline()` 里 `row >= 25` 时调用并回置 `row = 24`，逻辑正确 | | 4. `\t` 行尾 / `\b` 行首不越界 | ✅ `\t` 的 `(col+8)&~7 >= 80` 已走 `vga_newline()`；`\b` 有 `col > 0` 保护 | 顺手把计划书里列的另外两条"已知问题"也修掉了：`vga_clear()` 的行列复位已移出循环体，`\t` 越界已处理。 ## 还差 ❌ **1. 硬件光标（唯一的实质缺口）** `io.h` 已经写好 `outb`/`inb` 了，但**没有任何地方调用**——这大概就是你做到一半停下的地方。 要点提示（不代写）： - 端口对是 `0x3D4`（索引寄存器）/ `0x3D5`（数据寄存器） - 光标位置是 16 位，拆成两次写：先向 `0x3D4` 写索引 `0x0E`、向 `0x3D5` 写**高字节**；再写索引 `0x0F …（完整内容见原始记录）
