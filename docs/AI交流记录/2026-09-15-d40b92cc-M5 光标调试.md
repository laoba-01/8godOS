# 2026-09-15 — M5 光标调试

> 会话 ID `d40b92cc`｜队员消息 20 条 / AI 回复 22 条
> **AI 回复已截断至 600 字符**，完整原文见 `原始记录/d40b92cc*.jsonl`。
> 时间戳为 UTC（本地为 +8 小时）。

---

**[2026-09-15 03:48] 队员：**

看看做晚的M5跑完没有?

**[2026-09-15 03:50] AI：**

没跑完。昨晚停在"模块拆好了、能编能跑",但 M5 的四条验收标准**一条都没达标**。 ## 我实际验的(不是看文档说的) 重新 `make clean && make` + QEMU 起 ISO 截图: | M5 验收标准 | 结果 | 证据 | |---|---|---| | ① `make` 零警告通过 | ❌ | `vga.c:53` unused parameter `fmt`;`vga.c:5` `VGA_WIDTH` 和 `vga.c:6` `VGA_HEIGHT` 定义未使用 | | ② 30 行整屏上滚 | ❌ | `vga.c` 里**根本没有 `vga_scroll()`**,行满后继续写直接越过 `0xB8000+4000` | | ③ 硬件光标跟随 | ❌ | 全文件没有 `0x3D4`/`0x3D5` 的踪迹 | | ④ `\t` 行尾 / `\b` 行首不越界 | ⚠️ 一半 | `\b` 的 `if(vga_col>0)` 是对的;`\t` 有问题,见下 | 屏幕上确实打出了三行(截图我存成 `build/screen.png` 了),但那只是 M3 的能力,和 M5 无关: ``` Hello, kernel! This is 8GodOS M5: VGA terminal ``` ## ④ 为什么算没通过 `vga.c:36` 的 `vga_c …（完整内容见原始记录）

**[2026-09-15 03:53] 队员：**

那我接下来该做什么

**[2026-09-15 03:53] AI：**

按依赖顺序来,顺序不能反 —— 因为 scroll 的正确性建立在"行列不变式成立"之上,先做 scroll 等于在流沙上盖楼。 ## 第 0 步:先加 `src/io.h`(为第 3 步铺路) 硬件光标要写 `0x3D4`/`0x3D5` 两个端口,内核里现在没有任何 I/O 端口原语。建议单开 `src/io.h`,里面放 `static inline` 的 `outb`/`inb` —— 别塞进 `vga.c` 做 static,因为后面键盘(0x60)、PIC(0x20/0x21)全都要用,这是 M6 之后的基础设施。 ## 第 1 步:修不变式(标准 ①、④) 只改 `vga.c`,不新增功能: - 三处硬编码 `80`/`25` 换成 `VGA_WIDTH`/`VGA_HEIGHT` —— 顺手消掉那 3 个警告 - `vga.c:41` 的 `==` 改 `>=`,或者更好:让 `\t` 自己先判断 `vga_col + 8 > VGA_WIDTH` 就先换行、`vga_col=0`,再对齐。**根治点在 `\t`**,`>=` 只是兜底 - `vga_clear()` 里 `vga_row=0; vga_col=0;` 挪出循环体 做完这一步单独验一次:临时在 kmain 里打一串 `\t\t\t\t\n` 和行尾 tab,截图确认光标位置对。**别跳过验证**—— …（完整内容见原始记录）

**[2026-09-15 04:03] 队员：**

io.h里面放什么

**[2026-09-15 04:03] AI：**

一个 header-only 的端口 I/O 原语层。内容比你想的少 —— M5 只用到 `outb`,但顺手把 `inb` 一起放进去(键盘一上来就要)。 ## 放什么 ```c #ifndef IO_H #define IO_H #include <stdint.h> /* 向端口写一个字节 */ static inline void outb(uint16_t port, uint8_t val); /* 从端口读一个字节 */ static inline uint8_t inb(uint16_t port); #endif /* IO_H */ ``` 就这两个。函数体你自己写,下面是写之前必须知道的东西。 ## 为什么是 header + `static inline` 这是这一步唯一真正需要你决策的地方。三条路: | 方案 | 代价 | |---|---| | `io.h` 里 `static inline`(推荐) | 每个用到它的 .c 各生成一份指令,但 `out` 就一条指令,编译器直接内联,零额外开销。**不用改 Makefile 的链接列表** | | `io.c` + `io.h` | 要加 `io.o` 到 Makefile 的 `$(KERNEL)` 依赖里,否则链接报未定义符号 | | 宏 | 参数求值多次、无类型检查,不推荐 | 选 header- …（完整内容见原始记录）

**[2026-09-15 06:27] 队员：**

那io.c写什么

**[2026-09-15 06:28] AI：**

直接回答:**io.c 里什么都不写,把它删掉。** ## 为什么 你在 `io.h:6,8` 写的是 `static inline` 声明。这个组合有硬性语义:**函数体必须在同一个编译单元里可见**。`static` 的意思是"这个函数只有本 .c 用",`inline` 在这里是"如果没有被内联,本单元要留一份可调用的定义"。 所以把 body 挪进 `io.c` 会直接爆掉: - `io.c` 里有定义、`vga.c` 里只看见声明 → `vga.c` 调用时 GCC 报 `'outb' used but never defined`,链接期找不到符号 - 如果 `io.c` 里也写 `static inline`,那它和 `vga.c` 里那份就是**两个互不相干的函数**,各编一份,还是同一个结果 **`static inline` 的声明和定义不能分家。** 这是你现在这个空 `io.c` 最大的风险 —— 它看起来像"待填的坑",实际上是个不该存在的文件。 ## 两条自洽的路,选一条 | | A. header-only(推荐) | B. 真的分 .c | |---|---|---| | `io.h` | `static inline` + **完整函数体** | 只有普通原型 `void outb(uint16_t, uint8_t);` | | `io.c` | …（完整内容见原始记录）

**[2026-09-15 06:29] 队员：**

现在呢

**[2026-09-15 06:30] AI：**

只多了 `src/io.h:8` 的那个 `#endif`,别的没动。`src/io.c` 还在(0 字节),两个函数体还是空的。 ## 这次修好的 原来是 `#ifndef` 没闭合、预处理直接报 unterminated,现在排查掉了 —— 我实测: ``` gcc -ffreestanding -m64 -Wall -Wextra -fsyntax-only src/io.h → exit=0 ``` 这是唯一一处真进展。 ## 但还差得远,而且我实测了后果 我造了个假的 .c 调用这两个函数,看会发生什么: ``` 声明阶段:gcc -c → 4 条警告(declared 'static' but never defined / used but never defined) 链接阶段:gcc → ld: undefined reference to `outb' ld: undefined reference to `inb' (exit=1) nm -u: U outb U inb ``` **警告里那句「used but never defined」就是我上一轮说的坑,现在是实测复现了。** 现在这个文件处于一种会骗人的状态:`-fsyntax-only` 能过,看起来"没问题",一旦真有人调用就是链接失败。 还有个隐蔽点:**现在 `make` 完全看不见 io.h …（完整内容见原始记录）

**[2026-09-15 06:34] 队员：**

我都做好了,现在看看

**[2026-09-15 06:38] AI：**

磁盘上还是老样子 —— 三件事一件都没落。 ## 现在磁盘上真实的状态 | 我上轮要的 | 现在 | 证据 | |---|---|---| | 删掉 `src/io.c` | ❌ 还在 | `0 bytes, mtime=14:27:40`,从创建到现在没动过 | | 两个函数体填进 io.h | ❌ 没有 | 全仓库 grep `outb\|inb` 只命中 **2 行声明**,没有任何函数体 | | `#endif /* IO_H */` | ✅ 做了 | `io.h:8`,mtime 14:34:05 | io.h 从 156 字节变成 165 字节,差的那 9 个字节正好是 ` /*IO_H*/` —— **自上一轮以来,这个文件只发生了这一处改动。** `src/vga.c` 也没动过(mtime 还是 9月14 21:27),所以它还没 `#include "io.h"`。 顺带,`make` 现在的状态: ``` src/io.h:6: warning: 'outb' declared 'static' but never defined [-Wunused-function] src/io.h:8: warning: 'inb' declared 'static' but never defined [-Wunused-function] ``` `-fsyntax …（完整内容见原始记录）

**[2026-09-15 06:41] 队员：**

看看现在

**[2026-09-15 06:43] AI：**

io.c 删了 ✅,vga.c 加了 `#include "io.h"` ✅(+15 字节,分毫不差)。但 **io.h 还是没动** —— 165 字节,mtime 还是 14:34,函数体一个都没有。 ## 当前磁盘状态 | 项 | 状态 | |---|---| | `rm src/io.c` | ✅ 没了 | | `vga.c` include io.h | ✅ `vga.c:2` | | **io.h 填函数体** | ❌ **还是两行光秃秃的声明** | ## clean rebuild 实测 警告从 3 条涨到 **5 条**: ``` src/vga.c:53: warning: unused parameter 'fmt' src/io.h:6: warning: 'outb' declared 'static' but never defined src/io.h:8: warning: 'inb' declared 'static' but never defined src/vga.c:6: warning: 'VGA_HEIGHT' defined but not used src/vga.c:5: warning: 'VGA_WIDTH' defined but not used ``` **这不是退步,这是进步。** 在你加 `#include "io. …（完整内容见原始记录）

**[2026-09-15 06:44] 队员：**

直接上代码吧

**[2026-09-15 06:45] AI：**

明白,你明确开口了,那这次例外。先验后给 —— 我把代码放进临时文件按你们项目的 CFLAGS 编一遍,确认零警告再交给你。

**[2026-09-15 06:45] AI：**

验过了:0 警告 0 错误,反汇编出来就是 4 条指令,没有任何函数调用和外部符号。 ## `src/io.h` 完整内容 ```c #ifndef IO_H #define IO_H #include <stdint.h> /* 向端口写一个字节 */ static inline void outb(uint16_t port, uint8_t val) { __asm__ volatile ("outb %0, %1" : : "a"(val), "Nd"(port)); } /* 从端口读一个字节 */ static inline uint8_t inb(uint16_t port) { uint8_t ret; __asm__ volatile ("inb %1, %0" : "=a"(ret) : "Nd"(port)); return ret; } #endif /* IO_H */ ``` 反汇编证据(编译参数就是你们 Makefile 的 CFLAGS): ``` 10: ee out %al,(%dx) ← outb(0x3D4, 0x0F) 1b: ee out %al,(%dx) ← outb(0x3D5, 100) 1c: ec in (%dx),%al ← inb(0x3D5) 1f: ee out %al,(%dx) ← outb(0x3D4, v) ` …（完整内容见原始记录）

**[2026-09-15 06:47] 队员：**

我写好了，再检查一下,顺便回答一下，为什么这里是%1和%0

**[2026-09-15 06:48] 队员：**

[Request interrupted by user for tool use]

**[2026-09-15 06:49] 队员：**

你创建这个函数干嘛?

**[2026-09-15 06:49] AI：**

那个文件是临时的,不是给你的项目用的 —— 我想验证"如果 `%0`/`%1` 写反了,汇编器会不会拦下来",好让答案有实测依据而不是我嘴上说。放 `build/` 里是因为它被 gitignore 了,但**我不该不问一声就往你仓库里丢文件**,抱歉。这个验证不是必需的,我不做了,直接回答你的问题。 ## io.h 检查结果:通过 ✅ ```c __asm__ volatile ("outb %0, %1" : : "a"(val), "Nd"(port)); __asm__ volatile ("inb %1, %0" : "=a"(ret) : "Nd"(port)); ``` 和我验证过的版本逐字一致。clean rebuild 后 **io.h 那两条警告消失了**,现在只剩 vga.c 那 3 条老警告(`fmt`、`VGA_WIDTH`、`VGA_HEIGHT`),它们是第 1 步要收拾的,和 io.h 无关。 只还欠一行 Makefile:`$(BUILD_DIR)/vga.o: src/vga.c src/vga.h src/io.h` —— 还没加。 ## 为什么是 `%1` 和 `%0` 这是**两件独立的事**叠在一起,拆开看就不神秘了。 ### 第一件:GCC 的编号规则 —— 输出排在前面 `%N` 不是"第 N 个输入",它是**整个操作数列表从 0  …（完整内容见原始记录）

**[2026-09-15 06:59] 队员：**

那makefile你直接帮我写吧

**[2026-09-15 07:00] AI：**

改完了,两行,并且实测过了。 ## 改动 ```diff -$(BUILD_DIR)/vga.o: src/vga.c src/vga.h | $(BUILD_DIR) +$(BUILD_DIR)/vga.o: src/vga.c src/vga.h src/io.h | $(BUILD_DIR) -$(BUILD_DIR)/kernel.o: src/kernel.c | $(BUILD_DIR) +$(BUILD_DIR)/kernel.o: src/kernel.c src/vga.h | $(BUILD_DIR) ``` **第二条你没让我改,我主动加了,原因说明一下:** 上一轮验证时我顺手碰了 `vga.h`,结果 `make` 只重编了 `vga.o`、**没有重编 `kernel.o`** —— 而 `kernel.c:1` 明明 `#include "vga.h"`。同一个 bug,同一个文件,我手里已经有证据,再留着让你下轮踩一次没道理。你要是觉得越界了,回退掉就是。 ## 实测 ``` 碰 io.h → 重编 vga.o + 重链接 ✅ 不多不少 碰 vga.h → 重编 kernel.o + vga.o + 重链接 ✅ 两个都含 vga.h 的都编了 ``` 改前 `kernel.o` 那条是漏的,现在补上了。 ## 一个永久解,你自己判断要不要上 这种"手 …（完整内容见原始记录）

**[2026-09-15 07:01] 队员：**

看看第一步怎么写

**[2026-09-15 07:02] AI：**

先更正我上一轮说错的一句:我说"用 `VGA_WIDTH`/`VGA_HEIGHT` 替掉硬编码,**顺手把 3 个警告全消掉**" —— **这是错的,只能消 2 条**。第 3 条 `vga.c:53` 的 `unused parameter 'fmt'` 跟硬编码没关系,得单独治。刚实测: ``` void f(const char *fmt, ...) { } → warning: unused parameter 'fmt' void g(const char *fmt, ...) { (void)fmt; } → 零警告 ✅ ``` ## 核心:先立一个漏斗 这一步真正的设计动作,是在改任何东西之前,先给"换行"这件事建**唯一入口**: ```c /* 所有"换行"的路径都必须走这里 —— 第 2 步的 scroll 也只加在这一处 */ static void vga_newline(void) { vga_col = 0; vga_row++; /* 第 2 步在这里插入:if (vga_row >= VGA_HEIGHT) vga_scroll(); */ } ``` 放在 `vga_entry` 之后。现在 `\n` 和自动换行是 `vga.c:34` 和 `vga.c:41` 两条各写各的路径,scroll 一进来必然出现"一条滚了、一条没滚"。**先收敛, …（完整内容见原始记录）

**[2026-09-15 07:11] 队员：**

可以了吧,下一步

**[2026-09-15 07:12] AI：**

还不行 —— **`vga_newline()` 只有调用,没有定义**。构建现在是坏的: ``` src/vga.c:34:22: warning: implicit declaration of function 'vga_newline' vga.c:(.text+0x11c): undefined reference to `vga_newline' ``` `build/` 里**没有 `kernel.elf`**,`os.iso` 也没了 —— 链接断了,kernel.elf 现在根本产不出来,更别说跑。 ## 你写对的部分(大部分都对) | 改动 | 状态 | |---|---| | `vga_clear`:用 `VGA_WIDTH*VGA_HEIGHT` | ✅ | | `vga_clear`:复位挪出循环 | ✅ `vga.c:21-22` | | `\n` 走 `vga_newline()` | ✅ 调用写了 | | `\r` / `\b` | ✅ | | `\t` 先判 `>= VGA_WIDTH` 再换行 | ✅ `vga.c:36-39` | | `default`:`VGA_WIDTH`、`>=`、补 `break` | ✅ | | `vga_printf` 加 `(void)fmt` | ✅ | **3 条老警告全清了**(`VGA_WIDTH`/`V …（完整内容见原始记录）

**[2026-09-15 07:13] 队员：**

可以了吗

**[2026-09-15 07:14] AI：**

找到了 —— 你把调用点的警告用**加声明**消掉了,但函数体还是没有。 ## 发生了什么 ``` src/vga.h:34: void vga_newline(void); ← 你 15:13 加的 src/vga.c:34,38,44: vga_newline() ← 三个调用点 grep 全 src: 没有第四个匹配 ← 一个定义都没有 ``` 对比两次构建就很清楚了: | 上一轮 | 这一轮 | |---|---| | `warning: implicit declaration of function 'vga_newline'` | **警告消失了** | | `undefined reference to 'vga_newline'` | `undefined reference to 'vga_newline'` | **警告是会消失的,链接错误不会。** 声明只是告诉编译器"这个函数存在,你先别管",于是编译器闭嘴了,然后链接器去找 —— 找不到,报错。同一个 0x11c 偏移,同一个未定义符号。 **这就是"加声明消警告"这个动作最危险的地方**:它让构建从"有一条警告"变成"看起来干净",但实际问题一步没动。顺带说一句,这也说明 M5 那条"`make` 零警告通过"的验收标准本身是弱的 —— 零警告不代表能链接。 ## 两处要改 **1. 删掉 `vga.h …（完整内容见原始记录）

**[2026-09-15 07:15] 队员：**

这下可以了吧

**[2026-09-15 07:15] AI：**

构建通了 —— 但**行为还没验**。先做个非侵入的验证:不动你的 `kmain`,直接把你的 `vga.c` 编进一个用户态测试程序,用 `mmap` 把 `0xB8000` 映射出来,这样能离线检查不变式。

**[2026-09-15 07:16] AI：**

可以了,这次是真的。**构建干净 + 运行时行为也验过了。** ## 构建 ``` make clean && make → 零警告(只剩 ld 那两条,不在 M5 范围内) build/kernel.elf 6840 bytes ✅ build/os.iso 5130240 bytes ✅ ``` `vga_newline` 是 `static`,`vga.h` 里的声明也撤了,grep 全 src 只有 1 处定义 + 3 处调用。 ## 运行时验证(我要做的那个非侵入测试) 没动 `kmain`,直接把你**真实的 `src/vga.c`** 用 `#include` 编进一个用户态程序,`mmap` 把 `0xB8000` 映射出来,离线驱动: ``` === 测试 A: 72 个点 + Tab + X === 结束后 row=1 col=1 row 0 |........................................................................ | row 1 |X | ``` 72 个点填满 0~71 列,Tab 跳到 80 → 触发换行 → `X` 落在 **row 1 col 0**。✅ 正是期望行为。 ``` === 测试 B: 列 0 处退格 === 结束后 row=0 col=0 ✅ 不动、不越界 ```  …（完整内容见原始记录）

**[2026-09-15 07:30] 队员：**

可以了

**[2026-09-15 07:31] AI：**

`vga_scroll()` 本身**完全正确**,`vga_newline` 的逻辑也对 —— 但**少了一个右花括号**,整个文件编译不过: ``` src/vga.c:78:3: error: expected declaration or statement at end of input src/vga.c:75:8: warning: 'vga_printf' defined but not used src/vga.c:67:8: warning: 'vga_puts' defined but not used src/vga.c:43:8: warning: 'vga_set_color' defined but not used src/vga.c:33:8: warning: 'vga_clear' defined but not used ``` ## 数一下括号 ``` 24: static void vga_newline(void) { ← 开 1 25: vga_col = 0; 26: vga_row++; 27: if (vga_row >= VGA_HEIGHT) { ← 开 2 28: vga_scroll(); 29: vga_row = VGA_HEIGHT - 1; 30: /* 第 2 步在这里插入:... */ ← 这行是旧占位注释, …（完整内容见原始记录）

**[2026-09-15 07:33] 队员：**

可以了吧

**[2026-09-15 07:34] AI：**

构建通了。现在跑 30 行滚动测试 —— 我先把**预期结果**写出来,再对比:30 行、每行末尾 `\n`,会触发 6 次滚动,所以屏幕上应该是 **row 0~23 = 第 07~30 行**(连续 24 行),**row 24 空白**,光标停在 row 24 col 0。

**[2026-09-15 07:34] AI：**

**完全命中预期。** row 0~23 正好是第 07~30 行,连续无空洞,row 24 空白,光标停在 `row=24 col=0`。 ``` 0|07 line-7 | ... |23|30 line-30 | 24| | ← 空白,光标在这 ``` **第 2 步完成,标准 ② 达标。** 6 次滚动内容全对,1920/2000 那个差一错误没踩到。 ## 进度 | 标准 | 状态 | |---|---| | ① `make` 零警告 | ✅ | | ② 30 行上滚、行号连续 | ✅ 刚验 | | ③ 硬件光标跟随 | ⬜ 第 3 步 | | ④ `\t` 行尾 / `\b` 行首不越界 | ✅ 第 1 步已验 | ## 第 3 步:`vga_update_cursor()` `io.h` 终于要有第一个消费者了。放在 `vga_newline` 之后(`vga_clear` 之前),同样是 **`static`,不进 `vga.h`**: ```c /* 把软件光标位置同步到 VGA 硬件光标 */ static void vga_update_cursor(void) { uint16_t pos = (uint16_t)(vga_row * VGA_WIDTH + vga_col); outb(0x3D4, 0x0E); /* 选索引寄存器 14:高字节 */ o …（完整内容见原始记录）

**[2026-09-15 07:36] 队员：**

vga_scroll存在的意义是什么

**[2026-09-15 07:36] AI：**

一句话:**因为显存只有 25 行,而输出是无限的。scroll 是把这个矛盾糊过去的唯一办法。** ## 不 scroll 会怎样 VGA 文本缓冲区就是 `0xB8000` 开始的那 4000 字节,`80 × 25 × 2`。这不是软件约定,是这块显卡的物理布局 —— 屏幕上第 25 行对应最后那 160 字节,没有第 26 行。 现在 `vga.c:60` 那个下标: ```c vga_buffer[vga_row * VGA_WIDTH + vga_col] ``` 当 `vga_row` 涨到 25,下标就是 `25*80 = 2000`,字节偏移 `4000` —— **正好越过缓冲区末尾**。你写进去的东西: - 屏幕上**看不见**(那片内存不对应任何可见单元格) - 但写操作**真的发生了**,踩的是 `0xB8000 + 4000` 之后的内存。那里是 VGA 的其他寄存器和显存的其他部分,踩坏了会出什么怪象完全看运气 所以第 2 步之前那个状态,不是"多出来的行看不见",是"**输出到第 26 行就开始悄悄破坏显卡状态**"。你计划书里那句"越界不崩,不代表没写坏"说的就是这个。 ## scroll 做的事:一个骗局 终端的本质是**一个内容不断向上流动的窗口**。硬件只给 25 行的固定窗口,于是: ``` 来第 26 行之前: [01][02][03]. …（完整内容见原始记录）

**[2026-09-15 07:40] 队员：**

那M5做完了吗

**[2026-09-15 07:41] AI：**

/tmp 被清掉了(WSL 重启),重建 harness 再验一遍:

**[2026-09-15 07:41] AI：**

没有。**还差第 3、4 两步,而且第 3 步一行都还没写。** ## 实测状态 ``` grep -rn "vga_update_cursor\|0x3D4" src/ → (无) src/kernel.c 202 bytes mtime=9月14 22:41 → 从没动过 ``` | M5 验收标准 | 状态 | 证据 | |---|---|---| | ① `make` 零警告 | ✅ | clean rebuild 只剩 ld 那两条 | | ② 30 行上滚、行号连续 | ✅ | 首行 `07`、末行(row23) `30`、row24 空白、光标 `row=24 col=0` | | ③ **硬件光标跟随** | ❌ **没写** | `0x3D4` 全 src 零命中 | | ④ `\t` 行尾 / `\b` 行首不越界 | ✅ | Tab 后 `X` 在 row1col0;行首退格 `row=0 col=0` | 补括号那一下是对的,`vga_newline` 现在两个大括号配平了(31 行关 `if`,32 行关函数),重跑测试三条全过,没改坏东西。 **第 3 步的代码我上一轮给过了**(`vga_update_cursor()`,放在 `vga_newline` 之后、`vga_clear` 之前,`static` 不进 `vga.h`,末尾在 `vga_cl …（完整内容见原始记录）
