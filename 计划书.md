# 简易 x86_64 操作系统内核 — 计划书

> 目标:从零实现一个**能跑起来的最小内核**:引导 → 进入 64 位长模式 → 在 VGA 屏幕上打印 `Hello, kernel!`。
> 本计划书是路线图,具体代码在实施阶段按里程碑逐步产出。

---

## 1. 项目目标

- **可运行**:一条命令编译、一条命令在 QEMU 里启动,肉眼看到 `Hello, kernel!`。
- **可理解**:每一段启动代码都有注释,不是黑盒照抄。
- **可扩展**:目录结构、构建脚本为后续(中断/键盘、内存管理、用户态)留好位置。

**第一版明确不做(YAGNI):** 中断与键盘、分页之上的内存管理、多任务、文件系统、用户态、高半区重定位。这些作为后续里程碑,不塞进最小版。

---

## 2. 技术选型(已定)

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 目标架构 | x86_64 长模式 | WSL 原生 gcc 直接编 ELF,零交叉编译配置 |
| 引导方式 | GRUB + Multiboot2 | 最稳最快跑通,精力集中在内核本身 |
| 开发语言 | C + 少量汇编 | 主流、资料多,汇编只用在启动/切换 |
| 运行环境 | QEMU(x86_64) | 免真机,支持 `-kernel` 直载调试 |
| 开发环境 | WSL2 Ubuntu 24.04 | gcc/make/ld 已就绪,只需补 3 个包 |

---

## 3. 开发环境(已核实)

### 3.1 现状

| 位置 | 工具 | 状态 |
|------|------|------|
| WSL2 Ubuntu | gcc 13.3.0 / make 4.3 / ld 2.42 | ✅ 已就绪 |
| WSL2 Ubuntu | nasm 2.16.01 / qemu-system-x86_64 8.2.2 / grub-mkrescue 2.12 | ✅ 已就绪 |
| Windows 原生 | MinGW gcc 8.1(PE 目标) | ⚠️ 不适合编内核,不用 |

> **结论:全程在 WSL2 Ubuntu 里做,Windows 侧只用来编辑/查看文件。**

### 3.2 环境补齐(一次性,✅ 已完成 2026-09-08)

```bash
# 在 WSL 里执行
sudo apt update
sudo apt install -y nasm qemu-system-x86 grub-pc-bin xorriso mtools
# 验证
nasm -v
qemu-system-x86_64 --version
grub-mkrescue --version
```

### 3.3 项目目录位置(二选一)

| 方案 | 路径 | 优点 | 缺点 |
|------|------|------|------|
| **A(推荐)** | WSL 里 `cd /mnt/d/8GodOS` | Windows 与 WSL 共享同一份文件,资源管理器可见、可用 Windows 编辑器 | 9p 文件系统编译偏慢,grub-mkrescue 偶有权限/速度问题 |
| B | WSL 原生 `~/osdev` | 编译最快、grub-mkrescue 最稳 | Windows 侧看不到文件 |

> 默认走 **方案 A**。若 grub-mkrescue 在 `/mnt/d` 上出问题,退路是:源码留在 `/mnt/d/8GodOS`,把 ISO 生成步骤放到 WSL 原生 `/tmp` 再拷回(构建脚本里自动处理)。

---

## 4. 系统架构与启动流程

```
BIOS
 └─> GRUB(读 multiboot2 头 → 加载内核 ELF 到内存 → 进入 32 位保护模式)
      └─> _start(汇编,32 位)
           1. 校验 EAX==0x36D76289(multiboot2 魔数),保存 EBX(信息指针)
           2. CPUID 检查是否支持长模式
           3. 建立页表(恒等映射前 2MB,用 2MB 大页)
           4. 开启 PAE → 置 EFER.LME → 加载 CR3 → 开启分页(CR0.PG)
           5. 加载 64 位 GDT → 远跳转到 64 位代码段
      └─> _start64(汇编,64 位)
           1. 设置栈指针 rsp
           2. 清空 .bss 段
           3. call kmain
      └─> kmain(C)
           1. vga_clear() 清屏
           2. vga_puts() 打印 "Hello, kernel!"
           3. hlt 死循环
```

**内存布局(第一版,恒等映射):**

- 内核加载地址:`0x100000`(1MB)
- 页表恒等映射:`0x00000000 ~ 0x00200000`(前 2MB,足够覆盖内核 + 栈 + BSS)
- VGA 文本缓冲区:`0xB8000`(80×25,每字符 2 字节:字符 + 颜色属性)

---

## 5. 目录结构

```
/mnt/d/8GodOS/
├── src/
│   ├── boot.asm      # multiboot2 头 + 长模式切换(32→64)+ 入口
│   ├── linker.ld     # 链接脚本(内核固定在 1MB,含 multiboot2 段)
│   ├── kernel.c      # kmain(只留启动逻辑)
│   ├── vga.c         # VGA 文本终端实现(M5 加入)
│   ├── vga.h         # VGA 接口与 16 色调色板常量(M5 加入)
│   ├── io.h          # 端口读写 outb / inb(M5 加入)
│   ├── isr.asm       # 异常 stub 宏 + common stub + stub 地址表(M6 加入)
│   ├── idt.c         # IDT 表 + 门构造 + 异常名表 + panic(M6 加入)
│   └── idt.h         # 门描述符与 regs_t 栈帧结构(M6 加入)
├── grub/
│   └── grub.cfg      # ISO 的 GRUB 菜单
├── build/            # 构建产物(git 忽略)
│   ├── kernel.elf
│   └── os.iso
├── Makefile          # 一键 build / run / debug
├── 计划书.md          # 本文件
└── .gitignore
```

---

## 6. 工具链与构建链

### 6.1 编译命令

```bash
# 汇编:multiboot2 头 + 启动
nasm -f elf64 src/boot.asm -o build/boot.o

# C:内核主体
gcc -ffreestanding -fno-stack-protector -fno-pie -mno-red-zone \
    -mno-mmx -mno-sse -mno-sse2 -mcmodel=large \
    -c src/kernel.c -o build/kernel.o

# 链接
ld -n -T src/linker.ld -o build/kernel.elf build/boot.o build/kernel.o
```

> 说明:`-ffreestanding` 去掉宿主环境依赖;`-fno-stack-protector` 避免引用 libc;`-mno-red-zone`/`-mno-sse` 避免 x86_64 默认约定带来的坑;`-mcmodel=large` 对恒等映射最保险。

### 6.2 打包与运行

```bash
# 生成 ISO(grub-mkrescue)
mkdir -p build/iso/boot/grub
cp build/kernel.elf build/iso/boot/
cp grub/grub.cfg   build/iso/boot/grub/
grub-mkrescue -o build/os.iso build/iso

# 运行
qemu-system-x86_64 -cdrom build/os.iso

# 调试捷径(免 ISO,multiboot2 直载,迭代最快)
qemu-system-x86_64 -kernel build/kernel.elf
```

---

## 7. 里程碑计划

每个里程碑都有明确的**产出**和**验证标准**,做完一个再进下一个。

**当前进度:M0~M6 已完成 ✅;M7 待开始。**

### M0 — 环境就绪 ✅(已完成 2026-09-08)
- **做什么**:在 WSL 里补装 nasm/qemu/grub,建目录骨架。
- **验证**:`nasm -v`、`qemu-system-x86_64 --version` 正常输出。
- **状态**:✅ 完成。nasm 2.16.01 / qemu-system-x86_64 8.2.2 / grub-mkrescue 2.12 均就绪,`src/`、`build/`、`grub/` 目录骨架已建。

### M1 — 能引导
- **做什么**:写 `boot.asm` 的 multiboot2 头 + 一个空的 `kmain`(先不打印),写 `linker.ld`,配好 GRUB。
- **验证**:QEMU 启动后**不崩溃、不黑屏重启**(GRUB 成功加载内核并进入 `_start`)。可在 kmain 里先 `hlt` 占位。

### M2 — 进入长模式
- **做什么**:补全 `_start` 的页表 + 长模式切换,`_start64` 设栈清 BSS,最终跳进 C 的 kmain。
- **验证**:从 kmain 里能执行(例如向 0xB8000 写一个固定字符),证明 64 位代码在跑。

### M3 — VGA 打印
- **做什么**:在 `kernel.c` 实现 `vga_puts()`,kmain 打印 `Hello, kernel!`。
- **验证**:QEMU 屏幕上清晰显示 `Hello, kernel!`,**本阶段即达成项目总目标**。

### M4 — 一键脚本 ✅(已完成)
- **做什么**:完善 `Makefile`(`make run` = 编译 + 生成 ISO + 启动 QEMU;`make debug` 带 `-S -s` 供 gdb 连接)。
- **验证**:`make run` 一条命令直接看到屏幕输出;`make clean` 清理产物。
- **状态**:✅ 完成。`make` / `make run` / `make clean` / `make iso` 均已实现并跑通。
  **注意 `make debug` 目标尚未添加** —— 当前走 ISO 引导,还用不上 `-kernel` 直载调试,等真正需要 gdb 时再补。

### M5 — VGA 终端 ✅(已完成 2026-09-15)
- **做什么**:把 VGA 代码从 `kernel.c` 拆到独立的 `src/vga.c` + `src/vga.h`;实现完整的终端行为 —— 换行/回车/制表/退格的边界处理、**行满时整屏上滚**、**硬件光标跟随**。
- **验证标准**:
  1. `make` 零警告通过(`-Wall -Wextra`)。
  2. 连续打印 30 行:整屏上滚、行号连续、内容始终停在最后一行。
  3. 硬件光标始终停在下一个待写位置。
  4. `\t` 在行尾、`\b` 在行首均不越界(文本缓冲区只有 4000 字节)。
- **不做**:`vga_printf` 推迟到第 6 步(接口已留在 `vga.h`)。
- **状态**:✅ 完成。四条标准的验证方式与结果:
  - **标准 1**:`gcc -Wall -Wextra` 编译 `kernel.c` / `vga.c` 均**零警告**。linker 仍有 2 条警告,不属本次范围,见下方遗留项。
  - **标准 2**:在 QEMU 里 `memsave 0xb8000 4000` 取出整块显存解码,行号 `09`~`29` **连续、无跳号无重复**,开头的 00~08 共 9 行被上滚顶掉,末尾留一行空白,属性字节全部为 `0x07`。
  - **标准 3**:CRTC 寄存器 `0x0F`/`0x0E` 读出 `0x80`/`0x07`,即位置 1920 = 第 24 行第 0 列,正是下一个待写位置;截图逐格统计非黑像素,三帧为 9→0→9,证明光标**确实被画出来且在闪烁**。
  - **标准 4**:第 10 个 `\t` 走的是换行分支(后续文字从下一行第 0 列开始);行首 `\b` 未下溢(上一行行尾没有多出字符)。
- **实施中踩到并解决的关键点**:
  - **硬件光标不在显存里**,而是在 CRTC 的一组寄存器里,靠 `0x3D4`(索引口)/`0x3D5`(数据口)间接访问;索引口**有状态**,每写一次数据口之前都要重新选一次寄存器。
  - **GRUB 交接给内核时 `0x0A` 的 bit5(光标关闭位)是置上的** —— 只写光标位置是看不到光标的,必须先把这一位清掉。QEMU 实测:该位为 1 时,任何闪烁相位都不绘制。
  - 光标位置的更新必须放在 `vga_putchar()` 的 `switch` **之后**,不能放进分支里:`\t` 分支会瞬时把 `vga_col` 顶到 80,在那一刻算出的位置落在缓冲区外。
- **遗留项(与 VGA 无关,另开提交处理)**:
  - `ld` 两条警告:`boot.o` 缺 `.note.GNU-stack` 段(隐含可执行栈);`kernel.elf` 有 RWX 段。修这两条要动 `boot.asm` / `linker.ld`(启动路径),故不在 M5 收尾时一并改。
  - `make debug` 目标仍未添加(见 M4)。
- **教训**:**越界不崩,不代表没写坏。** 以及:验证硬件状态时,读寄存器比看屏幕可靠 —— 屏幕上的"看不见"可能是闪烁相位,也可能是设备被关掉了。

### M6 — IDT 与 CPU 异常处理 ✅(已完成 2026-09-17)
- **做什么**:建 IDT(256 项,向量 `0~31` 填 CPU 异常,`32~255` 显式清零);用 NASM 宏生成全部 32 个异常 stub,经统一 common stub 保存现场后交给 C 侧**两张表**(`isr_stub_table[32]` 填门、`exception_names[32]` 取名字)分发;命中异常时 panic 打印诊断信息后停机。**第 0 步先实现 `vga_printf`** —— M5 推迟过来的欠账(`vga.h:33` 接口已留),它是 panic 输出的前置依赖。
- **验证标准**:
  1. `make` 零警告通过(`-Wall -Wextra`)。
  2. **第 0 步单独验**:`vga_printf` 用一行覆盖 `%c %s %d %u %x %p %%` 及零填充宽度的样本,`memsave 0xb8000` + `decode_vga.py` 解码后逐字比对通过。**这一步不过不进 IDT** —— 否则 panic 输出打歪了,分不清是 printf 的问题还是栈帧的问题。
  3. 四条用例各自触发,panic 打出正确的向量号、名称、错误码:

     | 用例 | 向量 | 触发方式 | 期望错误码 | 附加看点 |
     |------|------|----------|------------|----------|
     | `#DE` 除零 | `0x00` | `volatile int a=1,b=0; c=a/b;` | `0`(stub 补的 dummy) | 走"stub 补码"路径 |
     | `#UD` 非法指令 | `0x06` | `__asm__ volatile("ud2");` | `0`(同上) | 同上 |
     | `#GP` 通用保护 | `0x0D` | `mov $0x30,%ax; mov %ax,%ds` | **`0x30`** | 走"CPU 直接压"路径 |
     | `#PF` 缺页 | `0x0E` | 读 `0x300000` | `bit0 P=0` | `cr2 == 0x300000` |

     `#GP` 的 `error=0x30` 是**最强判别点**:它证明错误码来自 CPU(`0x30` 就是那个越界的段选择子,GDT 只有 3 项、limit=23),而不是 stub 补的 `0`。两条路径如果分反了,这一条立刻露馅。
  4. 取证复用现有工具链:`memsave 0xb8000 4000` → `decode_vga.py` 解码 → 文本逐字比对,**不新写脚本**。
- **不做**:
  - 不碰 PIC、不执行 `sti`。CPU 异常是**同步**产生的,不受 `IF` 影响 —— 全程 `cli` 状态下 `#DE`/`#GP`/`#PF` 照样进得来,这是边界能画干净的依据。
  - 不做 IST / TSS(`#DF` 用当前栈)。
  - 不做异常恢复:32 个向量全部 panic,没有内存管理一个都修不了。
  - 不验用户态异常(`#CP`/`#SS` 在 ring0 触发不了),但 32 个门照样建好。
- **落实中的关键点**:
  - **栈帧布局与 `regs_t` 字段顺序必须严格一致**,这是整个 M6 最容易写错的地方。从低地址往高:`[15 个通用寄存器][向量号][错误码/0][rip][cs][rflags]`。用 `_Static_assert(offsetof(regs_t, vector) == 15*8, ...)` 让编译器替你守这条规矩 —— 谁改了字段顺序、或在 common 里多压了一个寄存器,**编译期就炸**,不靠记性。
  - **x86_64 没有 `pusha`/`popa`**:15 个通用寄存器手动压、反序弹。
  - **错误码分两类**:`8, 10~14, 17, 21, 29, 30` 共 10 个由 CPU 压,其余 22 个不压。宏必须在后者里先补一个 dummy `0`,否则 common 看到的栈帧是错位的,症状是"**有些异常正常、有些异常 RIP 完全不对**",极难查。
  - **`iretq` 不是 `iret`**;进入 handler 时 `DF` 的值没有保证,common 里必须 `cld`。
  - **ring0 下 CPU 不压 `RSP`/`SS`**,所以 `regs_t` 不放这两个字段。panic 若要显示被打断的 rsp,用 `(uint64_t)&r->rflags + 8` 从栈帧推算 —— **文案上要说明它不是 CPU 压栈字段**,免得以后被人当成真字段用。
  - **`#PF` 的真实地址在 `CR2`,不在错误码里**,错误码只给访问类型(P/W/U 位)。panic 模板在 `vector == 14` 时多打一行 `cr2=`,非 `#PF` 不打。
  - **`32~255` 要显式清零**,不能留 `.bss` 垃圾:`.bss` 不保证为 0(见遗留项),留着就是 224 个垃圾描述符。`idt_init()` 只写 32 个门,剩下 224 项的清零循环别漏。
  - **common stub 的恢复 + `iretq` 路径要完整写出**,哪怕 M6 全都 panic 走不到 —— 这样 M7 加 PIC 时只差"加一张函数指针分发表"这一步,不用回头改汇编。
  - **不额外做 SSE 的 16 字节栈对齐调整**:两种异常路径的压栈总量都是 20 qword = 160 字节,16 字节对齐自然保持;`call isr_handler` 再压 8 字节返回地址后,函数入口 `RSP % 16 == 8`,正是 ABI 期望的入口状态。但这只说明"**异常入口之后,stub/common 这段不改变原有的对齐状态**",**不是"以后任意中断都永远不用管对齐"** —— 换 M7 外部中断、或 handler 复杂化时要重新审一遍。这个 `20 qword` 要在 common stub 里写注释记下来,以后加寄存器得重新数。
  - panic 输出固定成这个形状,便于文本比对:
    ```
    EXCEPTION 0x0E #PF Page Fault
    error=0x0000000000000000
    cr2=0x0000000000300000
    rip=0x0000000000100234 cs=0x0008 rflags=0x0000000000000092
    ```
    这顺带定义了 `vga_printf` 的最小能力集:转换 `%c %s %d %u %x %p %%`,长度修饰符 `l`/`ll`/`z`,标志只支持 `0` + 十进制宽度;不做左对齐/精度/浮点。`-nostdlib` 下**没有 `vsnprintf` 可用**,必须自己 `va_arg` 逐字符吐到 `vga_putchar`(`stdarg.h` 是编译器提供的头,freestanding 下能用)。
  - **一次运行只能验一条用例**:异常处理器返回不到故障指令之后(`iretq` 会重新执行那条出错指令),所以四次运行,建议在 `kernel.c` 用编译期宏切换。`#PF` 的触发写法里指针和结果都要 `volatile`,否则 `-O2` 会把整条访存优化掉,表现是"什么都没发生"。
- **实施中踩到并解决的关键点**:
  - **文档的期望值和实现对不上:panic 要打 `0x0E`,而 `%x` 是小写。** 上面四条用例的期望输出全是大写十六进制(`0x00`/`0x06`/`0x0D`/`0x0E`),但 `vga_printf` 按第 0 步的规格只有小写 `%x`。选择是**给 printf 补一个 `%X`**(大写十六进制),而不是改文档的期望值 —— 这样上面那张验收表原样可用,逐字比对不会因为大小写白跑一轮。`vga_put_uint()` 因此多了一个 `upper` 参数。
  - **`const char *p;` 声明了没初始化就被 `while (*p != '\0')` 解引用。** 这是实施中真实出现过的一版代码,而 **`-Wall -Wextra` 完全沉默** —— 编译能过,一跑就读野指针。同批还有:声明了 `va_list ap` 却没 `va_start`、写成 `case:'c':`(冒号位置错)、`va_arg` 一个参数都没传、每个 `case` 缺 `break`。**后三类编译器会报,前两类不会** —— 这条分界线值得记住。
  - **Task 3(单 stub)/ 4(32 个 stub)/ 5(panic 完整化)三步合并成一次实现、分步验证。** 三步写的是同一个 `isr_handler`,分开做等于把同一个函数重写三遍。**验证的层次没有省**:`nm` + `info registers` 查 IDT base(证明 `lidt` 装的是本表)→ `#DE`(证明 stub 链通)→ `#UD`(再证一道路径)→ `#GP`(证明两类 stub 没分反)→ `#PF`(证明 CR2 取对)。每步仍是独立证据,省掉的只是返工。
  - **`isr.asm` 末尾加 `section .note.GNU-stack noalloc noexec nowrite progbits` 确实让 `isr.o` 没有新增 ld 警告** —— 预测正确,`ld` 全程稳定在 M5 那两条。
  - **验证证据**:`nm` 查到 `idt` 在 `0x109010`,QEMU `info registers` 的 `IDT=` base 与之一致、limit=`0x0fff`;四条用例解码后逐字比对全部相符,其中 `#GP` 的 `error=0x30` 正是那条越界的段选择子 —— 它只能来自 CPU,证明"带错误码 / 不带错误码"两类 stub 没有分反。
- **遗留项**:
  - **`_start64` 未清 `.bss`**(§4 承诺过,`boot.asm:81-92` 里没有这段)。GRUB 的 ELF 加载器会处理 `p_memsz > p_filesz` 的零填充,所以现状**大概率**是干净的,但为自举确定性应补上。补之前要先用 `linker.ld` 导出 `__bss_start`/`__bss_end`,否则循环没有边界符号。
  - **`#DF` 没有 IST 保护栈**:内核栈本身坏掉(溢出)时,`#DF` 处理器会因为同一个坏栈二次失败 → triple fault → 重启,**屏幕上看不到任何东西**。主内核栈变大时会需要它。
  - `ld` 的两条警告(M5 遗留)仍在。`isr.asm` 里加的 `section .note.GNU-stack noalloc noexec nowrite progbits` 让 `isr.o` **没有新增**第三条,但彻底消掉前两条仍需动 `boot.asm`。
  - **取证工具失去了自己的正确性标尺**。`tools/decode_vga.py` 是重建的,重建时用它解 `build/baseline.bin`(M5 内核的显存 dump)自证正确 —— 但那个样本是构建产物、没有入库,已随 `make clean` 删除。现在只能靠"输出看起来对"判断解码器是否可信,**而 M6 全部四条用例的判定都建立在它之上**。补法:把一份 dump 作为固定样本提交到 `tools/fixtures/`,并留一条能重新生成它的命令。
- **教训**:**`-Wall -Wextra` 不是审阅者。** 未初始化指针、声明了 `va_list` 却没 `va_start` —— 编译器一声不吭;它管的是语法与类型,不管"该做的事有没有做完"。以及:**工具不能住在构建产物目录里** —— 取证脚本原在 `build/`(`.gitignore` 忽略),一次 `make clean` 就永久删掉了 `decode_vga.py` 等四个脚本和 M5 的全部证据文件:没有副本、不在 git、回收站也没有。凡是"丢了要重做"的东西,都不能放在会被批量删除的目录下。

---

## 8. 关键技术点速查(实施时参照)

### 8.1 Multiboot2 头常量

| 常量 | 值 |
|------|-----|
| 头魔数(header magic) | `0xE85250D6` |
| 引导魔数(GRUB 传进 EAX) | `0x36D76289` |
| 架构字段 | `0`(i386 保护模式) |
| 终止 tag | type=0, size=8 |
| 入口地址 tag | type=3, size=12 |

> multiboot2 头必须落在镜像**前 32768 字节**内(放到 `.multiboot2` 段、链接时置于最前即可稳妥满足)。

### 8.2 长模式切换序列(顺序不可错)

```
1. 检查 CPUID.0x80000001.EDX bit29(长模式支持)
2. 页表: PML4[0] → PDPT ; PDPT[0] → PDE(PS=1, 2MB 恒等映射)
3. mov CR3, PML4 地址
4. mov CR4, 置 PAE 位(bit5)
5. 置 EFER(MSR 0xC0000080)的 LME 位(bit8)
6. mov CR0, 置 PG 位(bit31) —— 开启分页
7. 加载 64 位 GDT
8. 远跳转 jmp 0x08:start64 —— 进入 64 位段
```

### 8.3 64 位 GDT

- 段描述符需置 **L 位**(bit 21 of 高 32 位)、清 **D/B 位**;代码段 type=0b1010(可执行/可读)、DPL=0、P=1。
- 代码段选择子 `0x08`、数据段选择子 `0x10`。

### 8.4 VGA 文本模式

- 缓冲区 `0xB8000`;字符偏移 = (行×80 + 列) × 2;低字节=ASCII,高字节=颜色属性(`0x07` 灰字黑底)。

---

## 9. 风险与应对

| 风险 | 表现 | 应对 |
|------|------|------|
| 长模式切换顺序/段描述符写错 | triple fault,黑屏或重启 | boot.asm 逐行注释;M2 单独验证(先写固定字符证明进 64 位);用 QEMU `-d int -no-reboot` 抓异常 |
| 页表未覆盖内核/BSS | 进长模式后立刻缺页崩溃 | 恒等映射前 2MB,链接脚本把内核/BSS/栈全部压在 2MB 内 |
| grub-mkrescue 在 /mnt/d 上异常 | ISO 生成失败或极慢 | 退路:ISO 在 WSL 原生 /tmp 生成后拷回(脚本自动处理) |
| Windows 侧误用 MinGW gcc | 编出 PE、链接报错 | 所有构建命令一律在 WSL 里跑,Makefile 里显式用 `gcc`/`ld`(Linux 版) |

---

## 10. 参考资料

- OSDev Wiki — [Bare Bones](https://wiki.osdev.org/Bare_Bones)、[Multiboot2](https://wiki.osdev.org/Multiboot2)、[Setting Up Long Mode](https://wiki.osdev.org/Setting_Up_Long_Mode)
- GRUB 官方 Multiboot2 规范(spec 里的 header 字段与 tag 格式)
- 参考实现:OSDev 的 x86_64 Bare Bones、blog_os(Philip Oppermann)

---

## 11. AI 工具使用声明

> 完整记录见 [`docs/AI-使用记录.md`](docs/AI-使用记录.md);交流原文见 [`docs/AI交流记录/`](docs/AI交流记录/)。

本项目自 2026-09-06 起使用 Claude Code(AI 编程助手)协作开发。虽为练手项目(非参赛作品),但自始按**全国大学生计算机系统能力大赛操作系统设计赛**的 AI 工具披露要求留痕。

**各里程碑 AI 参与范围:**

| 里程碑 | 内核代码 | 文档 |
|--------|----------|------|
| M0 | — | 计划书由 AI 起草 |
| M1 / M2 | **全部由队员手写** | README 由 AI 撰写 |
| M3 | **全部由队员手写** | 同上 |
| M5 | 队员先写并交 AI 审阅;工期紧张时(09-14、09-15)由队员**明确要求**交棒,AI 整写 `vga.h` / `vga.c` / `kernel.c` | 计划书、README 由 AI 更新 |
| M6 | 队员先独立编写 `vga_printf`,随后明确要求交棒,AI 完成后全部实现(`isr.asm` / `idt.c` / `idt.h` / `vga_printf`) | 规格、实施计划、回填由 AI 完成 |

**两个要点:**

- **本项目所有 git 提交的作者都是队员本人**(`laoba-01`),AI 生成的代码同样以队员名义提交。**来源不能从 git 元数据判断** —— 以上表、`docs/AI-使用记录.md` 的逐文件表、以及各源码文件顶部的来源声明为准。
- 使用的模型为 **DeepSeek**(`deepseek-v4-pro` / `deepseek-flash`),运行在 Claude Code 命令行客户端中;逐会话分布见 `docs/AI-使用记录.md` 第一节。
- AI 造成过一次**实际损失**,已如实登记在 `docs/AI-使用记录.md` 第四节:执行 `make clean` 时未先检查 `build/` 内容,误删取证脚本与 M5 证据文件(已重建并迁移到 `tools/`)。

---

## 附:后续里程碑(不在第一版,仅登记方向)

1. 高半区内核(0xFFFF…8000 起)+ `-mcmodel=kernel`
2. GDT/IDT + PIC + 键盘输入
3. 分页之上的物理/虚拟内存管理 + kmalloc
4. 定时器/多任务/系统调用/用户态
