# 简易 x86_64 操作系统内核 — 计划书

> 目标:从零实现一个**能跑起来的最小内核**:引导 → 进入 64 位长模式 → 在 VGA 屏幕上打印 `Hello, kernel!`。
> 本计划书是路线图,具体代码在实施阶段按里程碑逐步产出。

---

## 1. 项目目标

- **可运行**:一条命令编译、一条命令在 QEMU 里启动,肉眼看到 `Hello, kernel!`。
- **可理解**:每一段启动代码都有注释,不是黑盒照抄。
- **可扩展**:目录结构、构建脚本为后续(中断/键盘、内存管理、用户态)留好位置。

**第一版明确不做(YAGNI):** 中断与键盘、分页之上的内存管理、多任务、文件系统、用户态、高半区重定位。这些作为后续里程碑,不塞进最小版。

---

## 2. 技术选型(已定)

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 目标架构 | x86_64 长模式 | WSL 原生 gcc 直接编 ELF,零交叉编译配置 |
| 引导方式 | GRUB + Multiboot2 | 最稳最快跑通,精力集中在内核本身 |
| 开发语言 | C + 少量汇编 | 主流、资料多,汇编只用在启动/切换 |
| 运行环境 | QEMU(x86_64) | 免真机,支持 `-kernel` 直载调试 |
| 开发环境 | WSL2 Ubuntu 24.04 | gcc/make/ld 已就绪,只需补 3 个包 |

---

## 3. 开发环境(已核实)

### 3.1 现状

| 位置 | 工具 | 状态 |
|------|------|------|
| WSL2 Ubuntu | gcc 13.3.0 / make 4.3 / ld 2.42 | ✅ 已就绪 |
| WSL2 Ubuntu | nasm 2.16.01 / qemu-system-x86_64 8.2.2 / grub-mkrescue 2.12 | ✅ 已就绪 |
| Windows 原生 | MinGW gcc 8.1(PE 目标) | ⚠️ 不适合编内核,不用 |

> **结论:全程在 WSL2 Ubuntu 里做,Windows 侧只用来编辑/查看文件。**

### 3.2 环境补齐(一次性,✅ 已完成 2026-09-08)

```bash
# 在 WSL 里执行
sudo apt update
sudo apt install -y nasm qemu-system-x86 grub-pc-bin xorriso mtools
# 验证
nasm -v
qemu-system-x86_64 --version
grub-mkrescue --version
```

### 3.3 项目目录位置(二选一)

| 方案 | 路径 | 优点 | 缺点 |
|------|------|------|------|
| **A(推荐)** | WSL 里 `cd /mnt/d/8GodOS` | Windows 与 WSL 共享同一份文件,资源管理器可见、可用 Windows 编辑器 | 9p 文件系统编译偏慢,grub-mkrescue 偶有权限/速度问题 |
| B | WSL 原生 `~/osdev` | 编译最快、grub-mkrescue 最稳 | Windows 侧看不到文件 |

> 默认走 **方案 A**。若 grub-mkrescue 在 `/mnt/d` 上出问题,退路是:源码留在 `/mnt/d/8GodOS`,把 ISO 生成步骤放到 WSL 原生 `/tmp` 再拷回(构建脚本里自动处理)。

---

## 4. 系统架构与启动流程

```
BIOS
 └─> GRUB(读 multiboot2 头 → 加载内核 ELF 到内存 → 进入 32 位保护模式)
      └─> _start(汇编,32 位)
           1. 校验 EAX==0x36D76289(multiboot2 魔数),保存 EBX(信息指针)
           2. CPUID 检查是否支持长模式
           3. 建立页表(恒等映射前 2MB,用 2MB 大页)
           4. 开启 PAE → 置 EFER.LME → 加载 CR3 → 开启分页(CR0.PG)
           5. 加载 64 位 GDT → 远跳转到 64 位代码段
      └─> _start64(汇编,64 位)
           1. 设置栈指针 rsp
           2. 清空 .bss 段
           3. call kmain
      └─> kmain(C)
           1. vga_clear() 清屏
           2. vga_puts() 打印 "Hello, kernel!"
           3. hlt 死循环
```

**内存布局(第一版,恒等映射):**

- 内核加载地址:`0x100000`(1MB)
- 页表恒等映射:`0x00000000 ~ 0x00200000`(前 2MB,足够覆盖内核 + 栈 + BSS)
- VGA 文本缓冲区:`0xB8000`(80×25,每字符 2 字节:字符 + 颜色属性)

---

## 5. 目录结构

```
/mnt/d/8GodOS/
├── src/
│   ├── boot.asm      # multiboot2 头 + 长模式切换(32→64)+ 入口
│   ├── linker.ld     # 链接脚本(内核固定在 1MB,含 multiboot2 段)
│   ├── kernel.c      # kmain(只留启动逻辑)
│   ├── vga.c         # VGA 文本终端实现(M5 加入)
│   └── vga.h         # VGA 接口与 16 色调色板常量(M5 加入)
├── grub/
│   └── grub.cfg      # ISO 的 GRUB 菜单
├── build/            # 构建产物(git 忽略)
│   ├── kernel.elf
│   └── os.iso
├── Makefile          # 一键 build / run / debug
├── 计划书.md          # 本文件
└── .gitignore
```

---

## 6. 工具链与构建链

### 6.1 编译命令

```bash
# 汇编:multiboot2 头 + 启动
nasm -f elf64 src/boot.asm -o build/boot.o

# C:内核主体
gcc -ffreestanding -fno-stack-protector -fno-pie -mno-red-zone \
    -mno-mmx -mno-sse -mno-sse2 -mcmodel=large \
    -c src/kernel.c -o build/kernel.o

# 链接
ld -n -T src/linker.ld -o build/kernel.elf build/boot.o build/kernel.o
```

> 说明:`-ffreestanding` 去掉宿主环境依赖;`-fno-stack-protector` 避免引用 libc;`-mno-red-zone`/`-mno-sse` 避免 x86_64 默认约定带来的坑;`-mcmodel=large` 对恒等映射最保险。

### 6.2 打包与运行

```bash
# 生成 ISO(grub-mkrescue)
mkdir -p build/iso/boot/grub
cp build/kernel.elf build/iso/boot/
cp grub/grub.cfg   build/iso/boot/grub/
grub-mkrescue -o build/os.iso build/iso

# 运行
qemu-system-x86_64 -cdrom build/os.iso

# 调试捷径(免 ISO,multiboot2 直载,迭代最快)
qemu-system-x86_64 -kernel build/kernel.elf
```

---

## 7. 里程碑计划

每个里程碑都有明确的**产出**和**验证标准**,做完一个再进下一个。

**当前进度:M0~M4 已完成 ✅;M5 进行中 ⏳。**

### M0 — 环境就绪 ✅(已完成 2026-09-08)
- **做什么**:在 WSL 里补装 nasm/qemu/grub,建目录骨架。
- **验证**:`nasm -v`、`qemu-system-x86_64 --version` 正常输出。
- **状态**:✅ 完成。nasm 2.16.01 / qemu-system-x86_64 8.2.2 / grub-mkrescue 2.12 均就绪,`src/`、`build/`、`grub/` 目录骨架已建。

### M1 — 能引导
- **做什么**:写 `boot.asm` 的 multiboot2 头 + 一个空的 `kmain`(先不打印),写 `linker.ld`,配好 GRUB。
- **验证**:QEMU 启动后**不崩溃、不黑屏重启**(GRUB 成功加载内核并进入 `_start`)。可在 kmain 里先 `hlt` 占位。

### M2 — 进入长模式
- **做什么**:补全 `_start` 的页表 + 长模式切换,`_start64` 设栈清 BSS,最终跳进 C 的 kmain。
- **验证**:从 kmain 里能执行(例如向 0xB8000 写一个固定字符),证明 64 位代码在跑。

### M3 — VGA 打印
- **做什么**:在 `kernel.c` 实现 `vga_puts()`,kmain 打印 `Hello, kernel!`。
- **验证**:QEMU 屏幕上清晰显示 `Hello, kernel!`,**本阶段即达成项目总目标**。

### M4 — 一键脚本 ✅(已完成)
- **做什么**:完善 `Makefile`(`make run` = 编译 + 生成 ISO + 启动 QEMU;`make debug` 带 `-S -s` 供 gdb 连接)。
- **验证**:`make run` 一条命令直接看到屏幕输出;`make clean` 清理产物。
- **状态**:✅ 完成。`make` / `make run` / `make clean` / `make iso` 均已实现并跑通。
  **注意 `make debug` 目标尚未添加** —— 当前走 ISO 引导,还用不上 `-kernel` 直载调试,等真正需要 gdb 时再补。

### M5 — VGA 终端 ⏳(进行中)
- **做什么**:把 VGA 代码从 `kernel.c` 拆到独立的 `src/vga.c` + `src/vga.h`;实现完整的终端行为 —— 换行/回车/制表/退格的边界处理、**行满时整屏上滚**、**硬件光标跟随**。
- **验证标准**:
  1. `make` 零警告通过(`-Wall -Wextra`)。
  2. 连续打印 30 行:整屏上滚、行号连续、内容始终停在最后一行。
  3. 硬件光标始终停在下一个待写位置。
  4. `\t` 在行尾、`\b` 在行首均不越界(文本缓冲区只有 4000 字节)。
- **不做**:`vga_printf` 推迟到第 6 步(接口已留在 `vga.h`)。
- **已知问题(截至 M5 拆分提交)**:
  - 缺 `vga_scroll()`,行满后继续写会越过 `0xB8000 + 4000` 的缓冲区边界。
  - 未操作 `0x3D4`/`0x3D5`,硬件光标不更新。
  - `vga_clear()` 把行列复位写在循环体内(功能无误,冗余)。
  - `\t` 在 `vga_col >= 72` 时会把列顶到 80,同样越界。
  - 教训:**越界不崩,不代表没写坏。**

---

## 8. 关键技术点速查(实施时参照)

### 8.1 Multiboot2 头常量

| 常量 | 值 |
|------|-----|
| 头魔数(header magic) | `0xE85250D6` |
| 引导魔数(GRUB 传进 EAX) | `0x36D76289` |
| 架构字段 | `0`(i386 保护模式) |
| 终止 tag | type=0, size=8 |
| 入口地址 tag | type=3, size=12 |

> multiboot2 头必须落在镜像**前 32768 字节**内(放到 `.multiboot2` 段、链接时置于最前即可稳妥满足)。

### 8.2 长模式切换序列(顺序不可错)

```
1. 检查 CPUID.0x80000001.EDX bit29(长模式支持)
2. 页表: PML4[0] → PDPT ; PDPT[0] → PDE(PS=1, 2MB 恒等映射)
3. mov CR3, PML4 地址
4. mov CR4, 置 PAE 位(bit5)
5. 置 EFER(MSR 0xC0000080)的 LME 位(bit8)
6. mov CR0, 置 PG 位(bit31) —— 开启分页
7. 加载 64 位 GDT
8. 远跳转 jmp 0x08:start64 —— 进入 64 位段
```

### 8.3 64 位 GDT

- 段描述符需置 **L 位**(bit 21 of 高 32 位)、清 **D/B 位**;代码段 type=0b1010(可执行/可读)、DPL=0、P=1。
- 代码段选择子 `0x08`、数据段选择子 `0x10`。

### 8.4 VGA 文本模式

- 缓冲区 `0xB8000`;字符偏移 = (行×80 + 列) × 2;低字节=ASCII,高字节=颜色属性(`0x07` 灰字黑底)。

---

## 9. 风险与应对

| 风险 | 表现 | 应对 |
|------|------|------|
| 长模式切换顺序/段描述符写错 | triple fault,黑屏或重启 | boot.asm 逐行注释;M2 单独验证(先写固定字符证明进 64 位);用 QEMU `-d int -no-reboot` 抓异常 |
| 页表未覆盖内核/BSS | 进长模式后立刻缺页崩溃 | 恒等映射前 2MB,链接脚本把内核/BSS/栈全部压在 2MB 内 |
| grub-mkrescue 在 /mnt/d 上异常 | ISO 生成失败或极慢 | 退路:ISO 在 WSL 原生 /tmp 生成后拷回(脚本自动处理) |
| Windows 侧误用 MinGW gcc | 编出 PE、链接报错 | 所有构建命令一律在 WSL 里跑,Makefile 里显式用 `gcc`/`ld`(Linux 版) |

---

## 10. 参考资料

- OSDev Wiki — [Bare Bones](https://wiki.osdev.org/Bare_Bones)、[Multiboot2](https://wiki.osdev.org/Multiboot2)、[Setting Up Long Mode](https://wiki.osdev.org/Setting_Up_Long_Mode)
- GRUB 官方 Multiboot2 规范(spec 里的 header 字段与 tag 格式)
- 参考实现:OSDev 的 x86_64 Bare Bones、blog_os(Philip Oppermann)

---

## 附:后续里程碑(不在第一版,仅登记方向)

1. 高半区内核(0xFFFF…8000 起)+ `-mcmodel=kernel`
2. GDT/IDT + PIC + 键盘输入
3. 分页之上的物理/虚拟内存管理 + kmalloc
4. 定时器/多任务/系统调用/用户态
